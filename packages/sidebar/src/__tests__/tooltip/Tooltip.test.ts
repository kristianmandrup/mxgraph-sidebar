import { Tooltip } from "../..";
// import { editorUi } from "./mocks";

describe("Tooltip", () => {
  let instance;

  beforeAll(() => {
    instance = new Tooltip();
  });

  describe("instance", () => {});
});
