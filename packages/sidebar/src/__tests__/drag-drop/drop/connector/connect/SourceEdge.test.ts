import { SourceEdge } from "../../../../..";

describe("SourceEdge", () => {
  const opts = {};

  let instance;
  beforeEach(() => {
    instance = new SourceEdge(opts);
  });

  describe("instance", () => {
    describe("properties", () => {});
  });
});
