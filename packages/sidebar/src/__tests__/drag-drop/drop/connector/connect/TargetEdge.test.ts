import { TargetEdge } from "../../../../..";

describe("TargetEdge", () => {
  const opts = {};

  let instance;
  beforeEach(() => {
    instance = new TargetEdge(opts);
  });

  describe("instance", () => {
    describe("properties", () => {});
  });
});
