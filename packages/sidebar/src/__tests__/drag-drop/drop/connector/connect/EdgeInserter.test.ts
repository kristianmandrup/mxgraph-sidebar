import { EdgeInserter } from "../../../../..";

describe("EdgeInserter", () => {
  const opts = {};

  let instance;
  beforeEach(() => {
    instance = new EdgeInserter(opts);
  });

  describe("instance", () => {
    describe("properties", () => {});
  });
});
