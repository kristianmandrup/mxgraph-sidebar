import { DropCheck } from "../../../..";
import { editorUi } from "../../../mocks";

describe("DropCheck", () => {
  const opts = {};

  let instance;
  beforeEach(() => {
    instance = new DropCheck(editorUi);
  });

  describe("instance", () => {
    describe("properties", () => {});
  });
});
