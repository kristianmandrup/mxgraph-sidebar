import { DropTarget } from "../../../..";
import { editorUi } from "../../../mocks";

describe("DropTarget", () => {
  let instance;
  beforeEach(() => {
    instance = new DropTarget(editorUi);
  });

  describe("instance", () => {
    describe("properties", () => {});
  });
});
