import { DropArrows } from "../../../..";
import { editorUi } from "../../../mocks";

describe("DropArrows", () => {
  const opts = {};

  let instance;
  beforeEach(() => {
    instance = new DropArrows(editorUi);
  });

  describe("instance", () => {
    describe("properties", () => {});
  });
});
