import { DragSource } from "../../..";
import { editorUi } from "../../mocks";

describe("DragSource", () => {
  let instance;
  beforeEach(() => {
    instance = new DragSource(editorUi);
  });

  describe("instance", () => {
    describe("properties", () => {});
  });
});
